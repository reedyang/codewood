You are performing a CONTEXT CHECKPOINT COMPACTION. Produce a dense, retrieval-friendly summary that another LLM can use to seamlessly resume the task — not a user-facing response.

Write exactly six lines in this order, using the fixed field names below:
Goals: ...
Facts: Paths/Commands/Tool results: ...; Environment/Workspace: ...; Errors/Fixes: ...
Preferences: ...
Decisions: ...
Errors: ...
Next steps: ...

Use compact semicolon-separated clauses, not paragraphs. Preserve concrete, reusable details — exact tools, commands, files, paths, flags/values, repo names, environment specifics, exact error strings, and any one-off details that could change future behavior or retrieval results — and do not compress away useful specifics. Merge duplicates, but if a detail is uncertain, mark it as tentative instead of omitting it. Use None for any field with no useful content. Output body text only: no markdown title, no JSON, and do not repeat these instructions.
